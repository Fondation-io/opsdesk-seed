{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -Eq 'git[[:space:]]+commit'; then bash \"$CLAUDE_PROJECT_DIR/scripts/check-secrets.sh\"; else exit 0; fi"
          }
        ]
      }
    ]
  }
}
